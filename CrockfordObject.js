// Douglas Crockford Object Creation

function constructor(spec) {
  let {member} = spec,
  {other} = other_constructor(spec),
  method = function () {
    // member, other, method
  };
  return Object.freeze({
    method,
    other
  });
}
